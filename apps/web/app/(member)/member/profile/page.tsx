export default function MemberProfilePage() {
  return (
    <main className="mx-auto max-w-2xl px-4 py-8">
      <h1 className="text-xl font-bold">Hồ sơ thành viên 369</h1>
      <p className="mt-2 text-sm text-gray-500">
        Placeholder Phase 1 — gọi <code>GET /api/v1/members/me</code> để hiển thị mã thành viên,
        trạng thái duyệt, điểm tích lũy.
      </p>
    </main>
  );
}
